import { Routes } from '@angular/router';
import { TablePageComponent } from './features/tableCRUD/table-page/table-page.component';

export const routes: Routes = [
    { path: 'first', component: TablePageComponent },
];
